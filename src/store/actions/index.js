import { createNewCategory, updateCategory, deleteCategories } from './categoriesActions';
import { addLocation, updateLocation, deleteLocations } from './locationsActions';

export { 
    createNewCategory,
    updateCategory,
    deleteCategories,
    addLocation, 
    updateLocation, 
    deleteLocations
};